{
  "name": "your-project-name",
  "version": "1.0.0",
  "description": "Airtable Interface",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "node-fetch": "^2.6.1" 
  }
}
